@model ClothesShop.Domain.Entities.Product

@*<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary"> @Model.Price.ToString("# €")</span>
    </h3>

    @using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.Id)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Add to Cart"/>
        </div>
    }

    <span class="lead">@Model.Description</span>
</div>*@


<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        <img src="~/Content/placeholder320.png" alt="">
        <div class="caption">
            <h4 class="pull-right">@Model.Price.ToString("# €")</h4>
            <h4>
                <a href="#">@Model.Name</a>
            </h4>
            <p>@Model.Description</p>
        </div>
        <div class="ratings">
            <p class="pull-right">15 reviews</p>
            <p>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
            </p>
        </div>
        @Ajax.ActionLink("Add to Cart", "AddToCart", "Cart", new { id = @Model.Id, returnUrl = Request.Url.PathAndQuery}, new AjaxOptions { HttpMethod = "POST" }, new {@class = "btn btn-success btn-sm"})
    </div>
</div>